export enum Blockchain {
    BTC = "BTC",
    ETH = "ETH",
    SOL = "SOL",
}

export enum Chain {
	ETHEREUM = "ETHEREUM",
	BITCOIN = "BITCOIN",
	SOLANA = "SOLANA",
}

export enum Network {
    MAINNET = "MAINNET",
    TESTNET = "TESTNET",
}